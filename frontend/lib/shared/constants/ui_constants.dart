// ignore_for_file: constant_identifier_names

/// 分页大小
const int kDefaultPageSize = 20;
const int kMaxPageSize = 100;

/// 卡片最大宽度（PC 端响应式布局）
const double kCardMaxWidth = 480.0;
const double kContentMaxWidth = 1200.0;

/// 动画时长（毫秒）
const int kAnimDurationMs = 200;
const Duration kAnimDuration = Duration(milliseconds: kAnimDurationMs);

/// 侧边栏宽度
const double kSidebarWidth = 240.0;
const double kSidebarCollapsedWidth = 72.0;
