import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // TODO: Replace with proper widget test once DI is set up for testing.
    expect(1 + 1, 2);
  });
}
