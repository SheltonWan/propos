# uni-app 主要面向微信小程序 / HarmonyOS / App，不走浏览器，无需 vite proxy。
# 客户端直连后端：注意端口为 8080（容器实际监听端口）。
# 注意：
#   1. 微信小程序需在管理后台配置 request 合法域名（HTTPS），生产 IP+HTTP 仅在真机调试"不校验合法域名"模式下可用
#   2. iOS App 默认禁止 HTTP 明文请求，需在调试模式启用 ATS 例外或使用 HTTPS
VITE_API_BASE_URL=http://111.230.112.246:8080
VITE_USE_MOCK=false
