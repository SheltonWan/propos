# PropOS Phase 1 — 前端原型界面 TODO 清单

> **版本**: v1.0
> **日期**: 2026-04-12
> **依据文档**: REQUIREMENTS_VERIFICATION_WITH_GUIDE.md v1.0
> **适用范围**: `/frontend` React 原型（React 18 + Tailwind CSS + Shadcn/ui）
> **状态图例**: 🔲 待开发 / 🔧 需补全（🔄 部分实现）

---

## 使用说明

本文档从 282 条 Phase 1 需求中筛选出 **前端原型界面层面需要补全或新增的 TODO 项**，排除以下类别：

- 纯后端业务逻辑 / 数据模型约束 / 定时任务 / 安全策略
- 微信小程序 / uni-app 移动端 / Admin PC 端独立功能
- 推送通知 / 消息引擎等非界面功能

每条 TODO 标注：**优先级**（Must = PRD 必须 / Should = PRD 建议）、**涉及组件**、**具体工作描述**。

---

## 汇总

| 模块 | 🔲 待开发 | 🔧 需补全 | 小计 |
|------|----------|----------|------|
| M1 资产与空间 | 4 | 1 | **5** |
| M2 租务与合同 | 8 | 4 | **12** |
| M3 财务与NOI | 7 | 0 | **7** |
| M4 工单系统 | 1 | 0 | **1** |
| M5 二房东穿透 | 2 | 0 | **2** |
| **合计** | **22** | **5** | **27** |

---

## M1 资产与空间可视化

### 🔲 M1-07 — 参考市场租金字段 `[Must]`

- **涉及组件**: `UnitDetail.tsx`
- **工作描述**: 在单元详情页"基本信息"卡片中增加「参考市场租金（¥/㎡/月）」字段，支持运营人员编辑维护。该字段用于 NOI 看板空置损失测算。
- **验收标准**: 单元详情页展示参考市场租金数值，可编辑保存

### 🔲 M1-09 — 商铺差异化字段 `[Must]`

- **涉及组件**: `UnitDetail.tsx`
- **工作描述**: 当单元业态为 `retail`（商铺）时，新增「商铺扩展」卡片，展示：
  - 门面宽度（m）
  - 临街面（是/否 或 描述）
  - 层高（m）
- **参考**: 已有「写字楼扩展」卡片（工位数 / 分隔间数）的实现模式
- **验收标准**: 商铺类型单元详情页出现商铺扩展卡片，含上述 3 个字段

### 🔲 M1-10 — 公寓差异化字段 `[Must]`

- **涉及组件**: `UnitDetail.tsx`
- **工作描述**: 当单元业态为 `apartment`（公寓）时，新增「公寓扩展」卡片，展示：
  - 卧室数量
  - 是否有独立卫生间
- **验收标准**: 公寓类型单元详情页出现公寓扩展卡片，含上述 2 个字段

### 🔲 M1-18 — 按业态筛选过滤热区 `[Must]`

- **涉及组件**: `FloorPlan.tsx`
- **工作描述**: 在楼层平面图页面工具栏中增加「业态筛选」下拉/切换，支持按写字楼/商铺/公寓过滤可见热区。当前仅有图层模式切换（状态/二房东/NOI/到期），无业态维度过滤。
- **验收标准**: 工具栏出现业态筛选选项，选择后非对应业态房间色块灰化或隐藏

### 🔲 M1-24 — 改造记录上传照片 `[Must]`

- **涉及组件**: `UnitDetail.tsx`（新增改造记录对话框）
- **工作描述**: 在"新增改造记录"对话框中增加照片上传区域，支持改造前/改造后多张照片上传（参考路径 `renovations/{record_id}/{index}.jpg`）。
- **验收标准**: 新增对话框含照片上传组件，支持多张预览

### 🔲 M1-25 — 资产台账导出 Excel `[Should]`

- **涉及组件**: `Assets.tsx`
- **工作描述**: 在资产列表页顶部增加「导出」按钮，支持按业态分类导出全量 639 套单元数据到 Excel。
- **验收标准**: 列表页出现导出按钮，点击后弹出导出配置（业态筛选）并触发下载

### 🔧 M1-17 — 热区与 unit_id 绑定结构 `[Must]`

- **涉及组件**: `FloorPlan.tsx`
- **当前状态**: 原型用 mock 数据 `Room` 对象与 `RoomOccupancy` 绑定，结构完整
- **待补全**: 确保热区 SVG 元素 `data-unit-id` 属性与后端真实 `unit_id` 对接预留，而非仅以编号文本匹配。Mock 数据中 Room.id 需模拟 UUID 格式。
- **验收标准**: SVG 热区元素含 `data-unit-id` 属性绑定 UUID 格式标识

---

## M2 租务与合同生命周期管理

### 🔲 M2-14 — 免租期分费项配置 `[Must]`

- **涉及组件**: `Contracts.tsx`（新建合同表单 / 高级设置）
- **工作描述**: 当前免租期配置仅支持「整体免租」。需增加分费项配置：
  - 切换模式：「整体免租」/「按费项免租」
  - 按费项模式下，可勾选免租费项（租金 ✓ / 管理费 ✗ / 水电 ✗），即"仅免租不免管理费"
- **验收标准**: 免租配置区增加模式切换，按费项模式下出现费项勾选列表

### 🔲 M2-53 — 账单含税/不含税双重展示 `[Must]`

- **涉及组件**: `InvoiceDetail.tsx`、账单列表卡片
- **工作描述**: 账单详情和列表卡片当前仅显示单一金额。需同时展示：
  - 含税金额
  - 不含税金额
  - 税额（可折叠明细）
- **验收标准**: 账单详情页和列表卡片同时展示含税/不含税两个金额

### 🔲 M2-77 — 楼栋级 WALE 查询 `[Must]`

- **涉及组件**: `WALEDashboard.tsx`
- **工作描述**: 当前 WALE 看板支持组合级和业态级查询，缺少楼栋级下钻。需在"分业态卡片"下方增加"楼栋 WALE"区域，展示各楼栋独立 WALE 数值。
- **参考**: NOIDashboard.tsx 已实现楼栋下钻模式
- **验收标准**: WALE 看板可按楼栋查看各栋 WALE 数值

### 🔲 M2-87 — 免租后基准调整递增类型 `[Must]`

- **涉及组件**: `Contracts.tsx`（递增规则配置区）
- **工作描述**: 在递增类型下拉中增加「免租后基准调整」独立选项。选中后展示配置字段：
  - 免租期结束后的基准租金
  - 基准调整方式
- **验收标准**: 递增类型下拉含 7 个选项（新增"免租后基准调整"），选中后展示对应配置字段

### 🔲 M2-89 ~ M2-92 — 递增模板独立管理 `[Should]`

- **涉及组件**: 新建页面/组件
  - `EscalationTemplates.tsx` — 递增模板列表页
  - `Contracts.tsx` — 新建合同时增加"选用模板"按钮
- **工作描述**:
  1. **M2-89** 递增规则可保存为命名模板（从合同配置中"另存为模板"）
  2. **M2-90** 新合同可一键选用已有模板（模板选择下拉/弹窗）
  3. **M2-91** 递增模板列表页，按业态分类展示
  4. **M2-92** 每个业态可设定默认递增模板
- **验收标准**: 有独立递增模板列表页；新建合同时可选用模板；支持按业态分类和默认模板设定

### 🔲 M2-95 — 未来租金预测表 `[Should]`

- **涉及组件**: `ContractDetail.tsx` 或 `WALEDashboard.tsx`（新增子组件）
- **工作描述**: 基于合同递增规则，生成未来 N 年各期预测租金表格/折线图。展示：
  - 逐期（月/季/年）预测租金金额
  - 递增时间节点高亮
  - 累计租金收入
- **验收标准**: 合同详情或 WALE 看板中可查看未来租金预测表（表格 + 图表）

### 🔧 M2-41 — 押金变更审批人字段 `[Must]`

- **涉及组件**: `DepositLedger.tsx`
- **当前状态**: 含交易流水列表（时间、类型、金额、原因）
- **待补全**: 每条交易流水增加「审批人」字段显示
- **验收标准**: 押金交易流水列表每条记录展示审批人信息

### 🔧 M2-71 / M2-79 — 面积加权 WALE 并列展示 `[Must]`

- **涉及组件**: `WALEDashboard.tsx`、`Home.tsx`
- **当前状态**: `Home.tsx` 部分角色显示面积加权卡片，`WALEDashboard.tsx` 仅展示收入加权
- **待补全**:
  - `WALEDashboard.tsx` 总览区增加面积加权 WALE 卡片，与收入加权并列展示
  - `Home.tsx` 所有角色统一展示两种口径
- **验收标准**: WALE 看板和首页同时展示收入加权 / 面积加权两个 WALE 数值

### 🔧 M2-86 — 每 N 年递增独立类型选项 `[Must]`

- **涉及组件**: `Contracts.tsx`（递增规则区域）
- **当前状态**: 固定比例递增支持填"每 N 年"间隔，但无独立类型选项
- **待补全**: 在递增类型下拉中将「每 N 年递增」作为独立选项（不再依附"固定比例"），配置字段含：递增间隔(年) + 递增比例(%) 或递增金额(¥)
- **验收标准**: 递增类型下拉含独立的"每 N 年递增"选项

---

## M3 财务与业财一体化

### 🔲 M3-40 — NOI 应收/实收口径切换 `[Must]`

- **涉及组件**: `NOIDashboard.tsx`
- **工作描述**: 在 NOI 看板顶部增加口径切换开关（Toggle/Tab）：
  - **应收口径**：基于账单应收金额计算 NOI
  - **实收口径**：基于实际到账金额计算 NOI
- 切换后所有指标（NOI、NOI Margin、收款率等）联动更新
- **验收标准**: NOI 看板含口径切换控件，切换后指标数据联动变化

### 🔲 M3-47 — K03 租户集中度指标 `[Must]`

- **涉及组件**: `KPISchemeForm.tsx`（指标库）
- **工作描述**: 在 KPI 指标选择区增加「租户集中度」指标（前 5 名租户租金收入占总收入比例）。类别归属 `finance`。
- **验收标准**: KPI 方案配置指标库出现"租户集中度"可选项

### 🔲 M3-50 — K06 空置周转天数指标 `[Must]`

- **涉及组件**: `KPISchemeForm.tsx`（指标库）
- **工作描述**: 增加「空置周转天数」指标（空置单元从空置到签约的平均天数）。类别归属 `leasing`。
- **验收标准**: KPI 方案配置指标库出现"空置周转天数"可选项

### 🔲 M3-52 — K08 逾期率指标 `[Must]`

- **涉及组件**: `KPISchemeForm.tsx`（指标库）
- **工作描述**: 增加「逾期率」指标（逾期账单金额 / 应收总额）。类别归属 `finance`。注意该指标为**反向指标**（越低越好）。
- **验收标准**: KPI 方案配置指标库出现"逾期率"可选项，标注为反向指标

### 🔲 M3-53 — K09 租金递增执行率指标 `[Must]`

- **涉及组件**: `KPISchemeForm.tsx`（指标库）
- **工作描述**: 增加「租金递增执行率」指标（按递增规则执行调价的合同数 / 应执行调价合同总数）。类别归属 `finance`。
- **验收标准**: KPI 方案配置指标库出现"租金递增执行率"可选项

### 🔲 M3-72 — 同比/环比显式标注 `[Should]`

- **涉及组件**: `KPIDashboard.tsx`（趋势图区域）
- **工作描述**: 当前趋势图含去年同期线，但无显式文字标注"同比 +X%" / "环比 +Y%"。需在趋势图 tooltip 或卡片中增加同比/环比数值标注。
- **验收标准**: KPI 趋势图或相关卡片含同比/环比百分比数值标注

### 🔲 M3-74 — KPI Excel 导出 `[Should]`

- **涉及组件**: `KPIDashboard.tsx`
- **工作描述**: 在 KPI 考核仪表盘增加「导出」按钮，支持将当期 KPI 考核结果导出为 Excel（含各指标得分、排名、趋势数据）。
- **验收标准**: KPI 仪表盘出现导出按钮，点击后触发 Excel 下载

---

## M4 物业运营与工单系统

### 🔲 M4-32 — 维修成本报表页面 `[Should]`

- **涉及组件**: 新建页面 `CostReport.tsx`
- **工作描述**: 新增维修成本报表页面，汇总展示：
  - 按月/季/年的总维修成本趋势图
  - 按维修类型（水电/空调/门窗/消防等）的费用分布饼图
  - 按楼栋/楼层的费用排名列表
  - OpEx / CapEx 分类汇总
  - 供应商费用排名
- **入口**: 工单模块导航中增加"成本报表"入口
- **验收标准**: 可从工单模块进入成本报表页，含趋势图、分布图、排名列表

---

## M5 二房东租赁信息穿透管理

### 🔲 M5-30 — 子租赁到期预警 `[Must]`

- **涉及组件**: `SubleasePenetration.tsx`
- **工作描述**: 在穿透分析看板中增加「到期预警」区域，展示：
  - 90 天内到期的子租赁列表
  - 按到期紧急程度色标（<30天=红、30-60天=橙、60-90天=黄）
  - 到期子租赁与主合同到期日的关系
- **验收标准**: 穿透分析看板含到期预警区域，按紧急程度色标展示即将到期子租赁

### 🔲 M5-32 — 楼层穿透视图热区 `[Should]`

- **涉及组件**: `FloorPlan.tsx` 扩展 或 新建 `SubleasePenetrationFloor.tsx`
- **工作描述**: 在楼层平面图的图层模式中增加「二房东穿透」视图（当前 FloorPlan.tsx 已有"二房东"图层模式的色块切换，但缺乏真正的穿透层级展示）。需展示：
  - 主合同承租区域 → 子租赁细分区域的嵌套关系
  - 子租赁空置 vs 已出租的色块区分
  - 点击热区弹出子租赁详情（终端租客、月租金、到期日）
- **验收标准**: 楼层平面图"二房东"图层模式下可见子租赁嵌套细分，含穿透详情弹窗

---

## 优先级排序建议

### P0 — Must 且影响核心业务数据展示

| 编号 | TODO 项 | 工作量估算 |
|------|---------|-----------|
| M1-09 | 商铺差异化字段 | 小（参照写字楼扩展卡片） |
| M1-10 | 公寓差异化字段 | 小（同上） |
| M1-07 | 参考市场租金字段 | 小（新增一个字段） |
| M2-53 | 账单含税/不含税双重展示 | 中 |
| M2-71/79 | WALE 两口径并列展示 | 中 |
| M3-40 | NOI 口径切换 | 中 |
| M5-30 | 子租赁到期预警 | 中 |

### P1 — Must 且影响操作完整性

| 编号 | TODO 项 | 工作量估算 |
|------|---------|-----------|
| M1-18 | 业态筛选过滤热区 | 小 |
| M1-24 | 改造记录照片上传 | 中 |
| M2-14 | 免租期分费项配置 | 中 |
| M2-41 | 押金审批人字段 | 小 |
| M2-77 | 楼栋级 WALE | 中 |
| M2-86 | 每 N 年递增独立选项 | 小 |
| M2-87 | 免租后基准调整递增 | 中 |
| M1-17 | 热区 unit_id 绑定 | 小 |
| M3-47/50/52/53 | KPI 缺失指标（4 项） | 小（指标库增加条目） |

### P2 — Should 或增强体验

| 编号 | TODO 项 | 工作量估算 |
|------|---------|-----------|
| M1-25 | 资产台账导出 Excel | 中 |
| M2-89~92 | 递增模板管理 | 大（新页面 + 组件） |
| M2-95 | 未来租金预测表 | 大（图表 + 表格） |
| M3-72 | 同比/环比标注 | 小 |
| M3-74 | KPI Excel 导出 | 中 |
| M4-32 | 维修成本报表 | 大（新页面） |
| M5-32 | 楼层穿透视图 | 大（FloorPlan 扩展） |

---

## 附：不在前端原型 TODO 范围内的 ⬜ 需求统计

以下类别的 ⬜ 需求已排除，需在各自模块中另行跟踪：

| 类别 | 条数 | 示例 |
|------|------|------|
| 后端业务逻辑 / 计算引擎 | ~50 | WALE 精确计算、FIFO 核销、递增联动 |
| 后端状态机 / 流程引擎 | ~20 | 合同提前终止、工单状态机、审核流程 |
| 后端定时任务 / 预警引擎 | ~20 | 信用评级重算、到期预警、催收提醒 |
| 后端安全 / RBAC / 审计 | ~15 | 行级隔离、权限控制、审计日志 |
| 后端数据模型约束 | ~10 | archived 标记、历史合同指向 |
| 微信小程序独立功能 | 4 | M4-13 ~ M4-16 |
| uni-app / Admin 独立功能 | ~10 | 二房东自助填报、管理后台录入 |
| 推送通知 / 消息渠道 | ~5 | APNs/FCM 推送、邮件通知 |
