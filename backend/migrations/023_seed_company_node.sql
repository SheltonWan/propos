-- =============================================================================
-- Migration: 023_seed_company_node
-- Description: 补充顶级企业节点（level=1）并将 020 中已建的三个部门关联至该节点
--
-- 背景：
--   020_seed_reference_data 以 parent_id = NULL, level = 2 插入三个部门，
--   schema 设计中 level=1 为公司级根节点，应当显式存在并被 level=2 部门引用。
--
-- 变量注入：企业名称通过 psql -v 参数传入，不得硬编码在本文件中。
--   由 scripts/setup_server.sh / scripts/init_local_postgres.sh 自动从以下
--   环境变量读取后注入：
--     COMPANY_NAME — 顶级企业名称（缺省 "PropOS 物业管理"）
--   手动执行时必须显式传入：
--     psql -v company_name='贵司名称' -f backend/migrations/023_seed_company_node.sql
-- 依赖: 002, 020
-- =============================================================================

BEGIN;

-- -------------------------------------------------------------------------
-- 1. 插入顶级企业节点（level=1）
--    固定 UUID，便于回滚与幂等校验
-- -------------------------------------------------------------------------
INSERT INTO departments (id, name, parent_id, level, sort_order)
VALUES (
    'de000000-0000-4000-8000-000000000001',
    :'company_name',  -- 从环境变量 COMPANY_NAME 注入
    NULL,
    1,
    0
)
ON CONFLICT (id) DO UPDATE
    SET name = EXCLUDED.name,
        updated_at = NOW();

-- -------------------------------------------------------------------------
-- 2. 将 020 中的三个部门（租务部/财务部/物业运营部）挂载到顶级节点
-- -------------------------------------------------------------------------
UPDATE departments
SET parent_id  = 'de000000-0000-4000-8000-000000000001',
    updated_at = NOW()
WHERE id IN (
    'de000000-0000-4000-8000-000000000002',
    'de000000-0000-4000-8000-000000000003',
    'de000000-0000-4000-8000-000000000004'
);

COMMIT;
