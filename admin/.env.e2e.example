# E2E 测试凭据配置模板
# 复制为 .env.e2e 并填写真实账号（不要提交 .env.e2e 到版本控制）
# Mock 轨道（playwright.config.ts）不需要此文件
# 真实后端冒烟测试（playwright.real.config.ts）需要此文件

E2E_USER_EMAIL=admin@propos.local
E2E_USER_PASSWORD=your_test_password_here
